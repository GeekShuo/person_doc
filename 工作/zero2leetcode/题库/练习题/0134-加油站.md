---
title: "134. 加油站"
difficulty: "Medium"
tags: [贪心, 数组]
source: "https://leetcode.cn/problems/gas-station/solutions/"
---

# 134. 加油站

**难度**：Medium | **分类**：贪心、数组 | **LeetCode**：[在线练习](https://leetcode.cn/problems/gas-station/solutions/)

## 题目描述

在一条环路上有 `n` 个加油站，其中第 `i` 个加油站有汽油 `gas[i]` 升。

你有一辆油箱容量无限的汽车，从第 `i` 个加油站开往第 `i+1` 个加油站需要消耗汽油 `cost[i]` 升。你从其中的一个加油站出发，开始时油箱为空。

给定两个整数数组 `gas` 和 `cost`，如果你可以按顺序绕环路行驶一周，则返回出发时加油站的编号，否则返回 `-1`。如果存在解，则保证它是唯一的。

#### 示例

```
输入：gas = [1,2,3,4,5], cost = [3,4,5,1,2]
输出：3
```

```
输入：gas = [2,3,4], cost = [3,4,3]
输出：-1
```

#### 提示

- gas.length == cost.length == n
- 1 <= n <= 105
- 0 <= gas[i], cost[i] <= 104

## 代码模板（Python）

```python
def can_complete_circuit(gas, cost):
    """
    :type gas: List[int]
    :type cost: List[int]
    :rtype: int
    """
    # 在这里写你的代码
    pass
```

## 测试用例

| # | 输入 | 期望输出 |
|---|---|---|
| 1 | `[[1, 2, 3, 4, 5], [3, 4, 5, 1, 2]]` | `3` |
| 2 | `[[2, 3, 4], [3, 4, 3]]` | `-1` |
| 3 | `[[5, 1, 2, 3, 4], [4, 4, 1, 5, 1]]` | `4` |

## 相关链接

- LeetCode：[https://leetcode.cn/problems/gas-station/solutions/](https://leetcode.cn/problems/gas-station/solutions/)
- 题解：https://leetcode.cn/problems/gas-station/solutions/
