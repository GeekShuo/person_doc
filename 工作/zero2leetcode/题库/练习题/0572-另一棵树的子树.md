---
title: "572. 另一棵树的子树"
difficulty: "Easy"
tags: [二叉树, 深度优先搜索]
source: "https://leetcode.cn/problems/subtree-of-another-tree/"
---

# 572. 另一棵树的子树

**难度**：Easy | **分类**：二叉树、深度优先搜索 | **LeetCode**：[在线练习](https://leetcode.cn/problems/subtree-of-another-tree/)

## 题目描述

给你两棵二叉树 `root` 和 `subRoot`，检验 `root` 中是否包含一棵与 `subRoot` 具有相同结构和节点值的子树。

#### 示例

```
输入：root = [3,4,5,1,2], subRoot = [4,1,2]
输出：True
```

```
输入：root = [3,4,5,1,2,null,null,null,null,0], subRoot = [4,1,2]
输出：False
```

#### 提示

- `root` 的节点数目范围是 `[1, 2000]`
- `subRoot` 的节点数目范围是 `[1, 1000]`
- `-104 <= Node.val <= 104`

## 代码模板（Python）

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

def is_subtree(root, sub_root):
    """
    :type root: TreeNode
    :type sub_root: TreeNode
    :rtype: bool
    """
    pass
```

## 测试用例

| # | 输入 | 期望输出 |
|---|---|---|
| 1 | `[[3, 4, 5, 1, 2], [4, 1, 2]]` | `true` |
| 2 | `[[3, 4, 5, 1, 2, null, null, null, null, 0], [4, 1, 2]]` | `false` |
| 3 | `[[1, 1], [1]]` | `true` |
| 4 | `[[1, 2, 3], [2, 3]]` | `false` |

## 相关链接

- LeetCode：[https://leetcode.cn/problems/subtree-of-another-tree/](https://leetcode.cn/problems/subtree-of-another-tree/)
- 题解：https://leetcode.cn/problems/subtree-of-another-tree/solutions/
