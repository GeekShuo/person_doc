---
title: "155. 最小栈"
difficulty: "Medium"
tags: [栈, 设计]
source: "https://leetcode.cn/problems/min-stack/"
---

# 155. 最小栈

**难度**：Medium | **分类**：栈、设计 | **LeetCode**：[在线练习](https://leetcode.cn/problems/min-stack/)

## 题目描述

设计一个支持 `push`、`pop`、`top` 以及在常数时间内检索最小元素的栈。

**本地练习约定：**为了适配单函数 OJ，这里使用函数 `min_stack(ops)` 来模拟操作：

- `ops` 是操作列表，如 `[["push",-2],["push",0],["getMin"],["pop"],["top"]]`
- 函数返回所有 `top/getMin` 操作的输出（按出现顺序）

#### 示例

```
输入：ops = [["push",-2],["push",0],["push",-3],["getMin"],["pop"],["top"],["getMin"]]
输出：[-3,0,-2]
```

#### 提示

- `push/pop/top/getMin` 总操作次数不超过 `3 * 10^4`

## 代码模板（Python）

```python
def min_stack(ops):
    """
    本地练习签名：
    :type ops: List[List[object]]
    :rtype: List[int]
    """
    # 在这里写你的代码
    # 建议：用两个栈，一个存值，一个存当前最小值
    pass
```

## 测试用例

| # | 输入 | 期望输出 |
|---|---|---|
| 1 | `[[["push", -2], ["push", 0], ["push", -3], ["getMin"], ["pop"], ["top"], ["getMin"]]]` | `[-3, 0, -2]` |
| 2 | `[[["push", 1], ["getMin"], ["push", 2], ["getMin"], ["pop"], ["getMin"]]]` | `[1, 1, 1]` |

## 相关链接

- LeetCode：[https://leetcode.cn/problems/min-stack/](https://leetcode.cn/problems/min-stack/)
- 题解：https://leetcode.cn/problems/min-stack/solutions/
- 博客详解：https://www.cnblogs.com/ranxi169/p/19766931
