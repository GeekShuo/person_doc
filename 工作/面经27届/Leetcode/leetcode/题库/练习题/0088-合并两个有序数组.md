---
title: "88. 合并两个有序数组"
difficulty: "Easy"
tags: [数组, 双指针, 排序]
source: "https://leetcode.cn/problems/merge-sorted-array/"
---

# 88. 合并两个有序数组

**难度**：Easy | **分类**：数组、双指针、排序 | **LeetCode**：[在线练习](https://leetcode.cn/problems/merge-sorted-array/)

## 题目描述

给你两个按**非递减顺序**排列的整数数组 `nums1` 和 `nums2`，另有两个整数 `m` 和 `n`，分别表示 `nums1` 和 `nums2` 中的元素数目。

请你合并 `nums2` 到 `nums1` 中，使合并后的数组同样按**非递减顺序**排列。

**说明：**最终结果存储在数组 `nums1` 中。`nums1` 的初始长度为 `m + n`，后 `n` 个元素为 0，仅用于占位。

#### 示例

```
输入：nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3
输出：[1,2,2,3,5,6]
```

```
输入：nums1 = [1], m = 1, nums2 = [], n = 0
输出：[1]
```

#### 提示

- nums1.length == m + n
- nums2.length == n
- 0 <= m, n <= 200

## 代码模板（Python）

```python
def merge(nums1, m, nums2, n):
    """
    :type nums1: List[int]
    :type m: int
    :type nums2: List[int]
    :type n: int
    :rtype: List[int]
    """
    # 在这里写你的代码（原地修改 nums1 后返回）
    pass
```

## 测试用例

| # | 输入 | 期望输出 |
|---|---|---|
| 1 | `[[1, 2, 3, 0, 0, 0], 3, [2, 5, 6], 3]` | `[1, 2, 2, 3, 5, 6]` |
| 2 | `[[1], 1, [], 0]` | `[1]` |
| 3 | `[[0], 0, [1], 1]` | `[1]` |

## 相关链接

- LeetCode：[https://leetcode.cn/problems/merge-sorted-array/](https://leetcode.cn/problems/merge-sorted-array/)
- 题解：https://leetcode.cn/problems/merge-sorted-array/solutions/
- 博客详解：https://www.cnblogs.com/ranxi169/p/19892041.html
