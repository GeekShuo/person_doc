# php中mysql使用

Owner: xqer

连接mysql

```
mysqli_connect(host, username, password, dbname,port, socket);
dbname 默认数据库
该函数成功后返回连接句柄
// 创建连接
$conn = new mysqli($servername, $username, $password);

```

关闭连接

mysqli_close ( mysqli $link )

没有指定link则关闭上一个打开的link

（脚本执行完毕后会自动关闭link）