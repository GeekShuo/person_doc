# 大模型各个岗位是在干什么？

Owner: xqer

模型训练 - 训练垂域场景大模型

大模型训练数据生成-过滤-合成-评估

数据生成： 

模型评估：构造各领域（如数学能力，结构化输出能力，京东自我认知，安全场景）包含输入问题和确定答案的数据集，跟大模型输出的结果对比，获得大模型的准确率数据，并排查准确率不高的原因（训练数据，训练方式，比如模型训练的时候输入的数据不够结构化，则输出也会不结构化）

工作难点：模型输出结果冗余，得提取出需要的字段。用多个try-catch也可能兜底兜不住。