1. 梳理了 Banana_instruct_caption 项目的完整代码逻辑，读完了全部源文件，写了约 24KB 的 README。搞清了试衣 LLM 评估体系的运作方式：三维五点评分（人物保真度、服饰一致性、图像质量）→ GSB 配对比较 → 可用率计算 → Bad Case 抽样标注，整个链路通过京东 AI API 调用 GPT-5/Gemini 3 实现。同时确认了九数试衣推理 API 的两阶段架构和调用方式。

2. 梳理了 qwen-image-train 项目三套训练脚本（全量微调、LoRA、Plus 版）的代码逻辑，写了约 23KB 的 README。搞清了 Flow Matching 训练范式（速度预测 + logit_normal 采样）、双图条件拼接机制、DeepSpeed ZeRO 分布式配置、CFG blank_prob 策略，以及两套数据格式（旧版 DreamBoothDataset vs 新版 ImgEditBaseDataset）的差异。

3. 盘点了共享存储 `/media/cfs/ea-cvfa-aigc-x2v-2/` 下的全部资源，包括各同事目录的模型/数据分布、model_hub（FireRed-1.0/1.1、Qwen-Image-Edit-2509/2511 等）、dataset_hub（Garments2Look、Tstars-VTON）、试衣输出图（约 9800 张）、158 个 Bad Case 等。

4. 跑通了九数试衣 API + LLM 评估的完整 pipeline（`run_tryon_eval.py`），过程中解决了 torch 在 B200 GPU 上不兼容（需 2.7+cu126）、API 大 payload 调用失败（需写 JSON 文件 curl -d @file）、embedding 服务 502 等问题。

**下周计划**

1. 修复 LoRA target_modules bug 并验证训练效果

2. 修复 2509 脚本 Resolution Bucket 死代码

3. 编写 Garments2Look → 统一格式转换脚本

4. 补充 Bad Case 分析，建立试衣质量回归检测