 

现有日均17.4万张法律文书图片，其中存在大量扫描不清、拍照虚焦、公章/水印遮挡、部分字过小导致字符不可辨的文书，无法完成后续一致性校验，应当拒单。但是不同的模糊程度，要有不同的处理结果，因此应该按文字可辨识程度分5档：

1. 完全清晰，任何字迹清晰可见
2. 字迹清晰可分辨，但有点模糊
3. 小部分字迹无法辨认
4. 大部分字迹无法辨认，只有部分可辨认
5. 完全无法辨认

## 1.现有方案的问题

此前已经有xgboost模型二分类（清晰86%/模糊14%），标注准确率约90%

只有1个标注人员，无法大规模人工标注

关键挑战：

1. XGBoost是二分类模型，训练目标只有清晰/模糊，其概率输出不适合多级划分

2. score值不线性对应视觉模糊程度。score=8.85的图片视觉上完全清晰，但被分到等级3

3. XGBoost输出的是0-100的blur_score，极度双峰分布（清晰集中在0.05-1，模糊集中在90-99），中间区间数据极少，难以直接切分5档阈值

4. 部分模糊样本被识别为清晰，数量不少，无法接受

 在以下几个方向进行了查找

- 图像质量评估(IQA)领域的无参考方法，如BRISQUE、NIQE、DBCNN
- 文档图像清晰度专用指标
- 多模态模型做图像质量评分的prompt工程
- 弱监督/半监督多级分类方法

## 2.做了什么探索，存在的gap

尝试及结果：

|   |   |   |
|---|---|---|
|方案|结果|失败原因|
|CLIP零样本分类|64.5%准确率|CLIP训练目标是图文语义匹配，不是图像质量评估，模糊和清晰的cosine相似度差异仅0.006|
|CLIP特征+线性分类头(5000条训练)|98.36%（vs XGB标签）|高分只是拟合了XGB弱监督标签。在82条分歧case上只有29.7%正确率，远低于XGB的70.3%。纯CLIP特征缺少OCR质量信息|
|独立测试集1000条验证|98.40%|仍然是vs XGB标签的一致率，不是真实准确率。证明没有过拟合但也没超越XGB|
|OCR置信度分档|只分出3档，等级4-5无法区分|RapidOCR对模糊图片有强容错性，即使严重模糊仍能检测到文字框并给出0.7-0.9的置信度，low_conf_ratio几乎全为0|
|VLM(Qwen3-VL-235B)直接评分|26%准确率|VLM能读出文字内容就判为清晰，从不给出4或5分。倾向给高分，无法做绝对评分|
|VLM两两对比|能分辨出差异|能识别相对清晰度，但不适合单独绝对评分，且大规模两两对比成本太高|

- XGBoost（569维特征：57维图像+OCR特征+512维CLIP）仍是当前最可靠方案
- 单一特征源都不够：纯CLIP缺OCR信息，纯OCR置信度对模糊不敏感，VLM倾向给高分
- 5档划分的瓶颈在于XGBoost的blur_score在中间区间(1-50)区分度不足

## 3.定义结合人工可辨和机器易识别的业务规则

双峰不是校准问题，而是训练目标导致的：二分类模型只学习“清晰/模糊”的决策边界，不会学习同一类别内部的严重程度。即使用等频、Platt 或 Isotonic 校准，也无法凭空补出等级2～4的信息。 当前定义存在空档：“小部分无法辨认”和“大部分无法辨认”之间，例如40%不可辨应该算哪档并不明确。 更合适的任务定义是：

可辨识度=文字不可辨认比例+可辨文字的清晰程度

也就是一个两阶段、两维度的序数任务，而非单一模糊分数。

改为可操作标准：

等级1 — 完全清晰

  文字笔画边缘清楚锐利，所有内容轻松可读，无任何模糊感。

等级2 — 轻微模糊

  字迹可辨认，但笔画边缘发虚/粘连/有轻微模糊感，不影响阅读内容。

等级3 — 局部模糊

  部分文字（10%-50%）看不清或难以辨认，其余部分清晰，不影响理解主要内容。

等级4 — 大部分模糊

  超过一半的文字（50%-90%）无法辨认，只能看清少量内容。

等级5 — 完全无法辨认

  90%以上文字不可辨认，几乎或完全无法阅读。

不确定

  无法判断，或图片特殊情况（如空白页、严重遮挡、非文书图片等）。

- 印章/签名/手写部分的模糊不计入等级判断，只看正文印刷文字
- 表格线、边框线断裂不算模糊
- 按正文文字的可辨认程度来判断，不是按整体图片美观度
- “可以根据上下文猜出来，但笔画看不清”应算不可辨认。

五档等级依赖模型两个输出头：

unreadable_ratio：不可辨文字比例；

readable_sharpness：仍可辨区域的清晰程度。这比强迫一个连续分数同时表达“轻微全局模糊”和“局部严重模糊”更稳定。

## 4. 数据构造

### 4.1 规则预先标注

对每行OCR结果，置信度 < 80% 的算"不可辨"。

权重 = 0.7 * 字数 + 0.3 * (文本框面积 / 图片面积 * 1000)

字数为主（70%），面积为辅（30%）。

unreadable_ratio = sum(weight where 行置信度 < 80%) / sum(all weights)

基础分档：

- ratio >= 90% 或 平均置信度 < 60 → 基础等级5
- ratio >= 50% 或 平均置信度 < 75 → 基础等级4
- ratio >= 10% → 基础等级3
- ratio < 10% → 进入阶段2区分等级1/2

当不可辨比例 < 10%时（文字基本可辨），用图像锐度指标区分：

满足以下任一条件 → 等级2（轻微模糊）：

- 平均置信度 < 92
- 最低行置信度 < 75
- 拉普拉斯方差 < 3000
- 文字边缘强度 < 100

否则 → 等级1（完全清晰）

### 漏检补偿

问题：模糊文字太糊时OCR检测器直接跳过，不产生文本框，导致不可辨比例偏低。

检测方法：

1. 用Otsu二值化提取文字像素，移除横线(40x1形态学开运算)和竖线(1x40开运算)
2. 用QRCodeDetector检测并移除二维码区域
3. 轻膨胀(5x5核)后做连通区域分析
4. 排除密度>0.90的实心块（背景/图标，非文字）
5. undetected_ratio = 不与任何OCR框重叠的区域bbox面积 / 所有文字区域bbox面积

注：用bbox面积而非像素面积，避免模糊文字膨胀连片导致的像素高估

补偿规则：

- 清晰图片的基线 undetected_ratio 约 0.05
- excess = undetected_ratio - 0.05
- excess > 0.15 → 等级+1
- excess > 0.30 → 等级+2

最终等级 = min(base_level + boost, 5)

### 4.2 人工标注

为了更好可视化标注结果、加快标注人员标注效率，减少无用工作，我开发了一套标注平台，支持搜索分页、缩放、快捷键等功能。标注人员根据五档业务规则，确定标签是否正确，并随时反馈 调整阈值（如印章检测的置信度，漏检补偿的规则）

### 4.3 微调vlm

前两部得到的数据为13,133行带人工标注的数据

### 4.3.1 数据:

      images/ 13,133 张图片

      train.json 9,850 条训练数据（75%，按appid分割）

      test.json 3,283 条测试数据（25%，独立appid）

      dataset_info.json LLaMA-Factory 数据集配置

    训练集等级分布:

      Level 1 (excellent): 5249 (53.3%)

      Level 2 (good): 3352 (34.0%)

      Level 3 (fair): 828 (8.4%)

      Level 4 (poor): 337 (3.4%)

      Level 5 (bad): 84 (0.9%)

由于level5和level4的样本数量最少，但是我们最关心的，为防止直接训练会导致模型偏好输出低level，使用平方根反频率过采样策略，拓展训练集到13523条。

### 4.3.2 生成软标签

- 等级映射: 你的 1(清晰)→excellent(5), 你的 5(无法辨认)→bad(1)
- 构造多种prompt

```
{
        "id": "ocr_4311_xxx.jpg",
        "image": "/abs/path/to/ocr_annotated/ocr_4311_xxx.jpg",
        "gt_score": 5.0,
        "gt_score_norm": 5.0,
        "level_probs": [0.95, 0.05, 0, 0, 0],
        "conversations": [
          {"from": "human", "value": "How would you judge the quality of this image?\n"},
          {"from": "gpt", "value": "The quality of the image is excellent."}
        ]
      }
```

- 软标签: 根据经验，固定 std=0.3 高斯，相邻等级有微小概率（~0.4%）
- WeightedRandomSampler: 权重=1/sqrt(class_freq)，Level 5 采样权重约 10x

### 4.3.3 lora微调

使用ms-swift框架，对qwen3.5-9B 进行微调

LoRA 配置: rank=8, lr=1e-4, batch=2, grad_accum=4, epochs=15, BF16 + SDPA, 冻结视觉塔和 aligner, 使用显式训练/验证 JSONL

其中遇到不少环境问题

训练总步数：2538

step eval loss

300 0.1046

600 0.0882

900 0.0772

1200 0.0757 ← 最优

1500 0.0828

1800 0.0935

2100 0.0979

2400 0.1172

2538 0.1185

结论：

eval loss 在 step 1200（约 1.4 epoch）就触底，之后一路上涨，从 0.0757 涨到 0.1185，涨幅超过 56%，是典型的过拟合。

最终 train loss 已经压到 0.01 附近、token_acc 0.996，而 eval loss 反而恶化，训练/验证差距很大。

```
### 1. 缺少 Qwen3.5 线性注意力 CUDA 依赖

安装了 `flash-linear-attention` 和 `causal-conv1d`。官方 `causal-conv1d` wheel 依赖更高版本 glibc，报 `GLIBC_2.32 not found`，因此用 GCC Toolset 12 针对 H20 `sm_90` 本地编译。编译产物已保存在 `.venv-swift`。

### 2. CUDA 12.1 不支持系统 GCC 14/13

源码编译先后被 GCC 14、13 阻塞，最终安装并使用 GCC Toolset 12 完成编译。

### 3. ms-swift 4.5 与 PyTorch 2.5 的 FSDPModule 冲突

先切换到官方 Qwen3.5 实践对应的 ms-swift 4.0.4。后续升级至 PyTorch 2.6 后已原生支持所需接口，临时兼容补丁已移除。

### 4. YAML 使用方法及 `ENV` 字段错误

YAML 必须通过 `--config` 加载。ms-swift 4.0.4 不支持 YAML 顶层 `ENV`，因此 GPU、DDP、显存分配和图像 token 环境变量由启动脚本设置。

### 5. H20 上 `libcublasLt` 触发 SIGFPE

PyTorch 2.5 + CUDA 12.1 在训练首个反向批次进入 `libcublasLt.so.12` 后触发 `Floating point exception`。这不是数据或 loss 错误，并可由特定矩阵稳定复现。

最终升级为 PyTorch 2.6.0 + CUDA 12.4，并完成以下验证：

- H20 问题矩阵的 FP16/BF16 前向与反向
- `causal-conv1d` CUDA 前向与反向
- FLA Gated DeltaNet CUDA 前向与反向
- 双卡训练、评估和 checkpoint 保存

启动脚本保留 `DISABLE_ADDMM_CUDA_LT=1` 作为额外保护。
```

## 5. 实验 xgboost vs clip+vlm 指出xgb的不足，做了调研、对比实验

### 6.后验 得到什么结果



# 文书清晰度规则检测 Pipeline

对公安、法院等机构上传的文书附件，自动下载、OCR 检测、清晰度定级、人工复核。文件路径 /apdcephfs_tj3/share_303745403/mikeypeng/qingxi/scripts/clarity

## Pipeline 总览

```
输入 CSV (appid, fpath, md5)
    │
    ▼
Step 1: 下载图片        step1_download.py
    │                   → images/ 目录
    ▼
Step 2: OCR + 定级      step2_ocr_classify.py
    │                   → result.csv (含等级+指标+定级理由)
    │                   → annotated/ (OCR标注图，可选)
    ▼
Step 3: 预览复核        step3_review_server.py
                        → Web UI (图片预览 + 人工标记)
                        → reviews.json (复核记录)
```

## 最小文件集合

```
qingxi/scripts/clarity/
├── step1_download.py      # 下载文书图片（API → 本地文件）
├── step2_ocr_classify.py  # OCR 检测 + 清晰度定级
├── step3_review_server.py # 预览复核服务（Web UI）
└── run_xpu.sh             # XPU 运行环境包装器（加速 OCR）
```

## 输入 CSV 格式

```
appid,fpath,md5
0301A2002431230123900FZ2601221731310,/antiFraud/attaches/01kfjgtaeq2s8znj10829btymc,a1b2c3d4e5f6...
```

|列|必填|说明|
|---|---|---|
|appid|是|业务 ID，用于关联复核记录|
|fpath|是|API 路径，如 /antiFraud/attaches/xxx（公安）或 /supreme/attaches/xxx（法院）|
|md5|否|文件 MD5，用于校验下载完整性（留空则跳过校验）|

## 使用方法

### Step 1: 下载图片

```
python step1_download.py input.csv ./images
```

- 调用下载 API http://9.147.113.141:13173/file/download，参数 bucketName=authority + destPath=fpath
- 返回 Base64 编码，按文件头自动判断格式（PDF/PNG/JPG），不做格式转换或压缩
- 下载后用 MD5 校验完整性，不匹配的标记但不删除
- 输出 images/ 目录 + images/download_report.csv 下载报告

### Step 2: OCR 检测 + 定级

```
# XPU 加速（推荐，~0.2s/张）
bash run_xpu.sh step2_ocr_classify.py --img-dir ./images --csv input.csv --out result.csv --annotate-dir ./annotated

# CPU 模式（较慢，~1s/张）
python step2_ocr_classify.py --img-dir ./images --csv input.csv --out result.csv --annotate-dir ./annotated
```

对每张图：

1. RapidOCR (paddle XPU) 跑全量检测，获取每行文字的坐标 + 置信度
2. 印章排除（HSV 红色占比 > 8% 的文本行不参与定级）
3. 计算指标：加权平均置信度、最低置信度、不可辨比例（conf<0.80 的行权重占比）、拉普拉斯方差
4. 按定级规则计算等级
5. 生成带 OCR 框的标注图（绿≥90% / 黄70-90% / 红<70%）

输出 result.csv，字段：

|字段|说明|
|---|---|
|appid|业务 ID|
|fpath|API 路径|
|filename|本地文件名|
|xgb_level|清晰度等级 (1-5)|
|ocr_avg_conf|加权平均置信度 (0-100)|
|ocr_min_conf|最低置信度 (0-100)|
|ocr_total_lines|文字行数（排除印章后）|
|ocr_low_conf_ratio|不可辨比例 (0-1)|
|ocr_unclear_boxes|模糊行数 (conf<0.80)|
|ocr_laplacian_var|拉普拉斯方差（锐度指标）|
|is_seal_excluded|排除的印章行数|
|reason|定级理由（中文描述）|

### Step 3: 预览复核

```
python step3_review_server.py --csv result.csv --img-dir ./images --annotate-dir ./annotated --port 8773
```

打开 http://<服务器IP>:8773/ 即可使用。

功能：

- 左侧列表：按等级排序，显示人工复核标签，支持筛选（未复核/全部/已复核）和搜索
- 右侧预览：原图 + OCR 标注框（可切换开/关），支持缩放（滚轮/双击）
- 定级理由：显示不可辨比例、置信度、拉普拉斯方差等指标和定级原因
- 人工复核：快捷键 1-5 标记等级，6 标记不确定，←/→ 翻页，U 撤销
- 导出：导出带复核结果的 CSV

## 定级规则

等级 1=最清晰，5=无法辨认。基于 OCR 置信度指标判定，漏检补偿已禁用。

|条件|等级|说明|
|---|---|---|
|不可辨比例 ≥ 90% 或 平均置信度 < 60|5|无法辨认|
|不可辨比例 ≥ 50% 或 平均置信度 < 75|4|大部分模糊|
|不可辨比例 ≥ 10%|3|局部模糊|
|平均置信度 < 92 或 最低置信度 < 75 或 拉普拉斯方差 < 3000|2|轻微模糊|
|以上均不满足|1|完全清晰|

指标说明：

- 不可辨比例：置信度 < 0.80 的文本行的加权占比（权重 = 0.7×字数 + 0.3×面积比×1000）
- 拉普拉斯方差：图像锐度指标，值越大越清晰
- 印章排除：红色印章上的文字不参与定级（HSV 红色占比 > 8%）

## 环境依赖

```
# 基础
pip install opencv-python numpy pandas pymupdf requests

# OCR (XPU 加速，需要 paddle-xpu 环境)
pip install rapidocr_paddle paddlepaddle-xpu

# OCR (CPU 版本)
pip install rapidocr_onnxruntime
```

XPU 环境配置见 run_xpu.sh，需要 paddle-xpu conda 环境。

注意：paddle-xpu 库中某个算子在 P800 旧 wheel 上没有注册，跑 rapidocr 时会报错，

需要禁用：envs/paddle-xpu/lib/python3.10/site-packages/rapidocr_paddle/utils/infer_engine.py

第 52 行加入 infer_opts.delete_pass("squeeze_excitation_fuse_pass")

## 下载 API 说明

- 地址：http://9.147.113.141:13173/file/download
- 参数：bucketName=authority + destPath=<fpath>
- 鉴权：AppId=yyptkfz, LoginName=taotzhu, Token=MD5签名
- 返回：Base64 编码的文件内容
- 格式判断：按文件头魔数（PDF: %PDF-, PNG: \x89PNG, JPG: \xFF\xD8）
- 红线：原始文件禁止 resize/有损压缩；下载脚本不做任何格式转换

# ocr预标签

## OCR预标签设计思路

为了节约人力成本，我首先使用OCR模型，通过其给出的置信度，结合一些图像特征，对当前的这些一万多条给出了一个预标签

初版规则如下：

[!

该规则中有几个关键设计

1.OCR框的置信度低于80%，则认为该框内文字不可辨、不可用

2.整体阈值档位划分为10% 50% 90%。之所以允许优秀档位有10%的不可辨文字，因为发现实际图像经常由于印章的存在降低可辨率，少有只有某一个特定地方模糊的文书，所以10%以下的都归位优质文书

3.增加了漏检补偿机制，许多文书有些部分过于模糊，直接没有被ocr模型框出来，所以要根据图像特征判断有没有漏检，漏检的图像质量降级

## 预标签结果V1

规则给出的标注结果，经过人工复核后如下

[![](data:image/svg+xml;base64,)ocr标注预标签复核](https://doc.weixin.qq.com/smartsheet/s3_AcIA0QY5AEkCNZqijdwS7RcuvM7B1?scode=AJEAIQdfAAoYusSHXL)

其中，ocr规则判断562条问题文书，其中有262条文书经过人工复核认为不应定为模糊。对这部分文书逐条复核原因，得到如下结论：

### 漏检补偿

漏检补偿是误报大头，很多文书可能背景比较黑，或者线条多，这部分被错误判断为了文字像素。之前的Otsu二值化是纯图像处理方法，它把所有暗像素都当成"可能是文字"，然后用连通区域分析来估算漏检。问题是法律文书里有很多非文字的暗区域——黑边框、签名、印章、装饰线、表格线残影——Otsu分不清这些和真正的文字，导致清晰图片也被判定为"有大量漏检文字"。

因此，尝试替换为DBNet方案：对同一张图跑两次DBNet检测，一次用正常阈值(0.5)只检测清晰文字，一次用低阈值(0.15)检测包括模糊文字在内的所有文字区域。两次结果的差值就是"模糊到差点检测不到的文字"面积。因为DBNet是训练过的文字检测器，它不会把黑边、签名、装饰图案当成文字。

替换为DBNet处理后，结果如下：

DBNet boost分布：0=289条（不补偿），+1=6条，+2=7条

    而旧的Otsu方法：302条里199条都在补偿（+1或+2），但其中绝大多数是误判——旧方法给出的漏检比例普遍在0.25-0.97，DBNet给出的几乎全是0.00-0.05。

DBNet 能排除的：

- 线框/表格线：DBNet天然不检测，因为线条没有文字的形态特征
- 二维码：DBNet天然不检测，黑白方块不是文字模式
- 黑色边框/暗背景：DBNet天然不检测
    
        DBNet 不能排除的：
    

- 印章：DBNet会把印章里的弯曲文字当成文字检测出来，需要后置过滤

现在剩下了77条模型判断为模糊的，实际为清晰的，精准54%-》87%，

但是小模型仍不能保证100%精准的误漏检补偿，手写文字签名、不同字体、蒙文，整体背景灰暗、反光、光晕等情况仍会出错，因此最终放弃使用漏检补偿，保证精准牺牲召回。

### 长图模糊

很多清晰的文书，由于下载的时候，分辨率过大被压缩，以及png→jpeg 导致压缩，变成了模糊文书，已经修改代码，校验md5 重新下载

### 长图小字OCR置信度偏低

OCR置信度在遇到手写体、楷体，等非黑体，以及小于20px高度，会偏低。对长宽比大于>2.5的case进行切块放大后ocr，把偏低的置信度变为正常

最终预标签结果模糊率3%左右，精准率100%