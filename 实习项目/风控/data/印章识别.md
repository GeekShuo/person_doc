

# 印章检测链路 (seal detection pipeline)

## 概述

对法律文书（公安/法院）进行印章检测，判断文档中是否包含印章。采用 三阶段级联 架构：PaddleX 三模型推理 → 8 块切分二次推理 → VLM 多页视觉判定，逐步缩小不确定范围。

---

## 链路架构

```
输入文档 (PDF/JPG/PNG)
    │
    ▼
┌─────────────────────────────────────────────┐
│  Stage 1: 三模型三分类 (seal_classify_online) │
│                                              │
│  下载文件 → 渲染所有页 → 逐页跑三模型         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐     │
│  │ layoutV2 │ │ layoutL  │ │ seal_det │     │
│  └──────────┘ └──────────┘ └──────────┘     │
│         │           │           │            │
│         └─────┬─────┘           │            │
│          V2/L 分数合并          │            │
│               │                 │            │
│     ┌─────────┴────────┐       │            │
│     │  classify_v5 规则 │←──────┘            │
│     │  + 颜色blob检测   │                   │
│     └────────┬─────────┘                    │
│              │                               │
│   HAS_SEAL / NO_SEAL / UNSURE                │
│                                              │
│   高长宽比图片(H/W>2.0)自动分块推理           │
└──────────────┬──────────────────────────────┘
               │
    ┌──────────┼──────────┐
    │ HAS_SEAL │ NO_SEAL  │ UNSURE
    │ (终态)   │ + UNSURE │
    │          │    │     │
    │          │    ▼     │
    │          │ ┌───────────────────────────────────┐
    │          │ │  Stage 2: 8块切分 (run_0804 S2)    │
    │          │ │                                    │
    │          │ │  重新下载 → 每页切8块(15%重叠)      │
    │          │ │  → 每块单独跑三模型 + classify_v5   │
    │          │ │  → 任一块 HAS_SEAL → HAS_SEAL      │
    │          │ └──────────┬────────────────────────┘
    │          │            │
    │          │    仍有 UNSURE
    │          │            │
    │          │            ▼
    │          │ ┌───────────────────────────────────┐
    │          │ │  Stage 3: VLM 多页判定             │
    │          │ │  (run_vlm_unsure_0804_v2)         │
    │          │ │                                    │
    │          │ │  渲染所有页 → 每批5页发VLM          │
    │          │ │  → 任一批检测到印章 → HAS_SEAL      │
    │          │ │  → 全部无印章 → NO_SEAL             │
    │          │ │  → VLM unknown → 保留 UNSURE       │
    │          │ └──────────┬────────────────────────┘
    │          │            │
    ▼          ▼            ▼
  最终合并: combined_results.json
    │
    ▼
  review_server (端口 8768) 人工审核
```

---

## 三个模型及其必要性

### 1. PP-DocLayoutV2 (layoutV2)

- 模型类型: 文档版面分析模型
- 作用: 检测页面中的各类区域（文字、表格、图片、印章 等），输出每个区域的类别和置信度
- 输出: seal 类别的检测框 + 置信度分数 (V2 分数)
- 必要性:

- 这是主力检测模型，对标准印章识别准确率最高
- V2 分数 >= 0.25 直接判为 HAS_SEAL（高置信度快速通道）
- 训练数据覆盖多种文档版面，对印章在页面中的位置有先验知识
- 相比 layoutL，V2 是更新版本，对小印章和彩色印章更敏感

### 2. PP-DocLayout-L (layoutL)

- 模型类型: 文档版面分析模型（大版本）
- 作用: 与 layoutV2 功能相同，但模型更大，检测粒度更细
- 输出: seal 类别的检测框 + 置信度分数 (L 分数)
- 必要性:

- 作为 layoutV2 的交叉验证，两个模型独立检测
- 当 V2 漏检但 L 检测到时（V2<0.01 但 L>=0.25），L 分数作为补充信号
- 当 V2 低分（0.01~0.25）时，需 L+辅助特征（seal_det 或 color blob）联合判定
- 两个模型互为补充，降低单模型漏检率

### 3. PP-OCRv4_server_seal_det (seal_det)

- 模型类型: 印章专用检测模型
- 作用: 专门检测圆形/椭圆形印章，输出多边形碎片 (dt_polys)
- 输出: 印章碎片框 + 分数，经聚类合并为印章区域
- 必要性:

- layout 模型基于版面分析，对非标准位置或模糊印章可能漏检
- seal_det 是印章专用模型，对印章的几何特征（圆形、弧线）更敏感
- 检测结果经聚类后，提取颜色/圆度/Hough变换等几何+颜色辅助特征
- 当 layout 分数处于中间区间 (0.12~0.25) 时，seal_det 的辅助特征是判定 HAS_SEAL 的关键依据
- 当 layout 分数极低 (<0.15) 时，seal_det 全部硬否决 + 无颜色 blob → 确信 NO_SEAL

### 模型间协同关系

```
V2/L 高分 (≥0.25)  → 直接 HAS_SEAL（无需其他模型）
V2/L 中分 (0.12~0.25) + seal_det/seal_det_geom 确认 → HAS_SEAL
V2/L 低分 (<0.15) + seal_det 全否决 + 无颜色 blob → NO_SEAL
其他情况 → UNSURE（交给 Stage 2/3）
```

### 颜色 blob 检测 (detect_color_seal_blobs)

非模型方法，纯 OpenCV HSV 颜色空间检测红/蓝/黄色印章斑块。作为第四路信号补充，在 layout 分数中低区间时提供额外确认。对黑白印章无效，但彩色印章场景下召回率高。

---

## classify_v5 三分类规则

|   |   |   |
|---|---|---|
|条件|判定|说明|
|V2>=0.25 或 L>=0.25|HAS_SEAL|layout 高分，直接判定|
|V2/L 中分(>=0.12) + seal_det确认 或 强颜色blob|HAS_SEAL|中分需辅助特征确认|
|V2/L 低分(<0.15) + seal_det全否决 + 无blob|NO_SEAL|确信无印章|
|V2/L 低分(<0.15) + 无强辅助特征|NO_SEAL|软否决|
|其他|UNSURE|不确定，交后续阶段|

---

## 各阶段详细说明

### Stage 1: 三模型三分类

- 脚本: seal_classify_online.py (process_doc) + run_0804.py --stage 1
- 输入: CSV 文件（doc_path 列），逐条下载
- 处理: 渲染 PDF 所有页 → 逐页跑三模型 → classify_v5 规则判定
- 分块: 高长宽比图片 (H/W>2.0) 按 A4 比例分块推理
- 汇总: 任一页/块 HAS_SEAL → 文档 HAS_SEAL
- 输出: infer_all.jsonl（每行一个文档结果）
- 速度: ~0.15 docs/s（XPU 单线程，瓶颈在 CPU 端 PDF 渲染+预处理）
- 注: 多进程加速无效（XPU-Util=0%，瓶颈在 CPU 端预处理，多进程争抢 CPU 核心反而变慢）

### Stage 2: 8 块切分

- 脚本: run_0804.py --stage 2
- 输入: Stage 1 结果中 NO_SEAL + UNSURE 的文档
- 处理: 每页切 8 块（15% 重叠）→ 每块单独跑三模型 + classify_v5
- 目的: Stage 1 可能因整页缩放导致小印章漏检，切块后增大印章在输入图中的占比
- 汇总: 任一块 HAS_SEAL → 文档 HAS_SEAL
- 输出: hybrid_8block.jsonl

### Stage 3: VLM 多页判定

- 脚本: run_vlm_unsure_0804_v2.py
- 输入: Stage 2 后仍为 UNSURE 的文档
- 处理: 渲染所有页 → 每批 5 页发 VLM (CSIG_QUANTUM_AGENT_SERVICE) → 逐页判定
- VLM 能力: 支持多图输入，每批 5 页约 6 秒，10 张以上效果下降
- 汇总: 任一批 has_seal=true → HAS_SEAL；全部 false → NO_SEAL
- 输出: unsure_vlm_labels_v2.jsonl

### 合并 & 展示

- 合并脚本: 内联在 execute_code 中，生成 combined_results.json
- 审核服务: review_server_0804.py，端口 8768

- 缩略图懒加载 (/api/thumb 端点，72 DPI JPEG ~3.5KB/张)
- 大图查看 (/api/img 端点，150 DPI PNG，支持多页翻页)
- 按 S1/S2/最终/VLM 四列对比
- 按分类/文件类型/页数/标注状态筛选
- 人工标注 + 备注

---

## 关键参数

|   |   |   |
|---|---|---|
|参数|值|说明|
|PDF 渲染 DPI|200|Stage 1/2 推理用|
|VLM 图片 DPI|150|Stage 3 VLM 判定用|
|小图放大阈值|500px|短边 <500px 放大到 1000px|
|高长宽比分块阈值|H/W > 2.0|超过此值按 A4 比例分块|
|Stage 2 切块数|8|正常比例图片纵向切 8 块|
|切块重叠|15%|相邻块重叠避免切到印章|
|VLM 每批图片数|5|超过 5 张效果下降|
|VLM 最大页数|50|超过 50 页的文档截断|
|layout 高分阈值|0.25|V2 或 L >= 0.25 直接 HAS_SEAL|
|layout 中分阈值|0.12|需辅助特征确认|
|layout 低分阈值|0.15|低于此值考虑 NO_SEAL|
|seal_det 碎片阈值|n_frag>=2, score>=0.9|有效印章碎片|
|颜色 blob 最小面积|200 px|红/蓝/黄印章斑块|

---

## 文件索引

```
scripts/seal/
├── seal_classify_online.py      # 核心推理：三模型加载 + classify_v5 规则 + process_doc
├── run_0804.py                  # 0804 实验：Stage1 (三分类) + Stage2 (8块切分)
├── run_vlm_unsure_0804_v2.py    # Stage3: VLM 多页判定 UNSURE
├── review_server_0804.py        # 审核服务 (端口 8768)
├── review_server_0727.py        # 0727 公安数据审核服务
├── seal_run.sh                  # paddle-xpu 环境启动脚本
├── download_batch.py            # 批量下载工具
├── download_all_and_dedup.py    # 全量下载 + MD5 去重统计
└── test_vlm_multi.py            # VLM 多图能力测试

experiments/seal/
├── 0727/                        # 公安数据实验
│   ├── data/
│   │   ├── dataset_v4_input.csv       # 输入 (13128 条, taotzhu 标注)
│   │   ├── combined_results.json      # 全量结果 (13128 条)
│   │   ├── combined_results_filtered.json  # 过滤后 (11528 条, UNSURE 已回写)
│   │   ├── unsure_vlm_labels.jsonl    # VLM 标注结果 (488 条)
│   │   └── unsure_qwen3_labels.jsonl  # Qwen3 标注结果 (477 条)
│   └── no_to_has_seal_single_page.md  # no→HAS_SEAL 误检分析
│
└── 0804/                        # 法院数据实验
    ├── data/
    │   ├── input.csv                   # 输入 (71464 条法院抽样文书)
    │   ├── infer_all.jsonl             # Stage1 结果 (24831 条)
    │   ├── hybrid_8block.jsonl         # Stage2 结果
    │   ├── unsure_vlm_labels_v2.jsonl  # Stage3 VLM 结果 (559 条)
    │   ├── combined_results.json       # 最终合并结果 (24831 条)
    │   ├── downloads/                  # 本地文件缓存 (24831 个)
    │   └── human_labels.csv            # 人工标注
    └── 法院抽样文书7w                   # 原始 TSV (71464 行)
```

---

## 实验结果

### 0727 公安数据 (dataset_v4, 13128→11528)

|   |   |   |
|---|---|---|
|分类|数量|占比|
|HAS_SEAL|12548→10958|95.6%→95.1%|
|NO_SEAL|103→570|0.8%→4.9%|
|UNSURE|477→0|3.6%→0%|

- 数据来源: taotzhu 用 Qwen 预标注的正负样本 (positive/negative 目录)
- 精准率 100%, 召回率 97.62%, F1 98.80%
- UNSURE 全部经 VLM 回写消解

### 0804 法院数据 (71464 抽样→24831 实跑)

|   |   |   |
|---|---|---|
|分类|数量|占比|
|HAS_SEAL|24021|96.7%|
|NO_SEAL|765|3.1%|
|UNSURE|0|0%|
|ERROR|45|0.2%|

- MD5 去重: 24831 → 16847 唯一文件 (重复率 32.2%)
- ERROR 原因: 全部为空 PDF (decode_none, 渲染后 std=0)
- VLM 多页 vs 单页: 多页找出 163 个有印章 vs 单页仅 15 个

---

## 环境与运行

```
# paddle-xpu 环境
XPU_ENV=/apdcephfs_tj3_303745403/share_303745403/mikeypeng/envs/paddle-xpu
PYTHON=$XPU_ENV/bin/python

# 模型路径
MODEL_ROOT=/apdcephfs_tj3/share_303745403/mikeypeng/models/paddleocr_vl_1.6

# Stage 1 + 2
cd scripts/seal
./seal_run.sh run_0804.py --stage 1
./seal_run.sh run_0804.py --stage 2

# Stage 3 (VLM, 不需要 XPU)
python3 run_vlm_unsure_0804_v2.py

# 审核服务
python3 review_server_0804.py --port 8768
```

### VLM API

- 端点: http://28.48.55.121:8021/v1/chat/completions
- 模型: CSIG_QUANTUM_AGENT_SERVICE
- 支持多图输入，每批建议 ≤5 张
- 耗时: 单图 3s，5 图 6s，10 图 ~12s

### 文件下载 API

- 端点: http://9.147.113.141:13173/file/download
- 参数: bucket=authority, destPath=doc_path
- 响应: base64 编码的文件内容
- 速度: 8 线程 ~19/s

### 硬件

- XPU: P800, 98304MiB VRAM (单模型副本约 16-20GB)
- CPU: 23 核 cgroup, 250GB 内存
- 瓶颈: CPU 端 PDF 渲染 + PaddleX 预处理 (XPU-Util=0%)

# 模型调研与端到端融合可行性分析

## 一、三个模型技术规格

|   |   |   |   |
|---|---|---|---|
|属性|PP-DocLayoutV2|PP-DocLayout-L|PP-OCRv4_server_seal_det|
|架构|RT-DETR + Pointer Network|RT-DETR-L|ResNet + FPN (PSE)|
|参数量|53.05M|32.26M|28.49M|
|文件大小|202.3 MB|123.0 MB|108.7 MB|
|输入尺寸|800x800 (固定resize)|640x640 (固定resize)|动态 (resize_long=736)|
|检测类别|23类 (含seal)|23类 (含seal)|仅印章弯曲文本|
|训练数据|论文/杂志/合同/书籍/试卷/研究报告|同左|500张圆形印章测试集|
|精度|未公开mAP|90.4% mAP@0.5|98.21% Hmean|
|预处理|resize到800x800, 不归一化|resize到640x640, 不归一化|resize_long=736, ImageNet归一化|
|后处理|NMS + 框分类|NMS + 框分类|DBPostProcess (多边形)|
|原始用途|通用版面分析+阅读顺序|通用版面分析(23类)|印章内弯曲文本检测|

关键发现：三个模型原本都不是专门做"印章有无判定"的。

- layoutV2 / layoutL 是通用版面分析模型，seal 只是 23 个类别之一，训练数据以论文/杂志/合同为主，法律文书占比未知
- seal_det 本质是"印章内文本检测"模型，检测的是弯曲文字而非印章本身，对无文字的纯图案印章无效

---

## 二、当前链路中各模型的实际贡献

基于 0804 法院数据 (24831 条) 的统计：

### HAS_SEAL 判定来源

|   |   |   |   |
|---|---|---|---|
|来源|数量|占比|说明|
|V2+L 都高分 (≥0.25)|12137|50.9%|两模型一致，最可靠|
|仅 L 高分|11337|47.6%|L 是主力，V2 漏检|
|仅 V2 高分|48|0.2%|V2 几乎不独立贡献|
|辅助特征驱动 (V2,L<0.25)|305|1.3%|靠 seal_det + 颜色blob|

### 核心发现

1. layoutL 承担了 98.5% 的 HAS_SEAL 检出（单独或与V2联合），V2 几乎不独立贡献
2. V2 和 L 在 46% 的文档上分歧严重（11385/24831），说明两个模型对印章的判断标准不一致
3. UNSURE 中 68% (415/607) 双模型完全无信号 (V2<0.01 且 L<0.01)，模型彻底漏检
4. VLM 在双模型零信号的 UNSURE 中找回 24 个有印章，这些是模型完全看不到的

### 各模型不足

#### layoutV2 (PP-DocLayoutV2)

|   |   |   |
|---|---|---|
|不足|数据证据|根因|
|对印章检出率极低|仅 0.2% 独立检出|训练数据中印章样本少，主要拟合论文/杂志版面|
|输入固定 800x800|大图缩放后小印章丢失|不保留原始分辨率|
|与 L 分歧率高|46% 文档分歧|两个模型检测标准不一致|
|额外参数浪费|53M 但 Pointer Network(阅读顺序)对印章无用|阅读顺序模块占 ~20M 参数，印章场景完全不需要|

#### layoutL (PP-DocLayout-L)

|   |   |   |
|---|---|---|
|不足|数据证据|根因|
|输入固定 640x640|比 V2 更小，小印章更容易丢|640 分辨率对 A4 文档，印章可能只占 20-30px|
|90.4% mAP 是 23 类平均|seal 单类精度未知|23 类均摊，印章可能远低于平均|
|仍有 2.5% 漏检 (607/24831)|UNSURE 中 68% 双模型零信号|非法律文书专用，对法院/公安文书泛化不足|
|无颜色感知|黑白印章和彩色印章一视同仁|纯视觉特征检测，不利用 HSV 颜色先验|

#### seal_det (PP-OCRv4_server_seal_det)

|   |   |   |
|---|---|---|
|不足|数据证据|根因|
|本质是文本检测不是印章检测|检测弯曲文字碎片|只能找有文字的印章，纯图案印章无效|
|后处理复杂|需聚类+颜色/圆度/Hough辅助判定|输出碎片而非完整印章框|
|辅助特征规则手工设计|classify_v5 中大量硬编码阈值|规则不可学习，难以泛化|
|贡献有限|仅 1.3% 的 HAS_SEAL 依赖它|大多数情况 layoutL 已能判定|
|98.21% Hmean 是 500 张测试集|实际法律文书表现未知|测试集可能与实际场景分布不同|

#### classify_v5 规则系统

|   |   |
|---|---|
|不足|说明|
|硬编码阈值|V2>=0.25, L>=0.25, 0.12, 0.15 等全是人工调参|
|规则不可学习|颜色blob、圆度、Hough 等特征的组合逻辑固定|
|无法端到端优化|模型训练和规则设计完全分离|
|场景过拟合|阈值针对当前数据调优，换场景可能失效|

---

## 三、端到端融合方案分析

### 方案 A: 微调单一 layout 模型 (最轻量)

思路: 用 layoutL (检出率最高) 做基础模型，在法律文书印章数据上微调

做法:

1. 收集法律文书 + 印章标注 (框标注，不是分类标签)
2. 用 PaddleDetection/RT-DETR 微调 layoutL
3. 只保留 seal 类别 (或加少量其他类别防止灾难遗忘)
4. 输出: seal 框 + 置信度，单阈值判定

优点:

- 改动最小，只需微调
- 单模型推理，速度快
- 保留 RT-DETR 的检测能力

缺点:

- 仍是固定分辨率输入 (640x640)，小印章问题不解决
- 需要框级标注数据 (当前只有文档级标签)
- 无法利用颜色等先验知识

预估数据需求: 2000-5000 张带印章框标注的法律文书

### 方案 B: 印章专用检测模型 (中等)

思路: 训练一个只做印章检测的专用模型，替换三个模型

做法:

1. 用当前 24831 条数据 (HAS_SEAL=24021) 的文档作为训练集
2. 标注印章位置 (框级)，训练 RT-DETR / YOLO 等检测器
3. 输入用更高分辨率 (1280x1280 或动态)
4. 多尺度推理 (原始 + 切块) 作为测试时增强

优点:

- 专注印章检测，精度可以做得更高
- 可选用更高分辨率输入
- 单模型替代三模型，简化部署

缺点:

- 需要大量框级标注 (当前只有文档级 yes/no 标签)
- 训练成本和时间

预估数据需求: 5000+ 张框级标注

### 方案 C: 二分类端到端模型 (最彻底)

思路: 直接训练一个"输入文档图片 → 输出有印章/无印章"的二分类模型

做法:

1. 用现有数据 (24831 条, HAS_SEAL=24021, NO_SEAL=765) 作为训练集
2. 采用多实例学习 (MIL) 或注意力机制:

- 文档逐页/逐块提取特征 (CNN backbone)
- 注意力聚合各页/块特征
- 输出二分类

4. 不需要框级标注，只需文档级标签

优点:

- 完全端到端，无需手工规则
- 直接优化目标 (有无印章)
- 可学习颜色、形状、位置等综合特征
- 无需框级标注

缺点:

- 正负样本极不平衡 (24021 vs 765, 去重后 16847 vs 222)
- 需要设计多页聚合机制
- 黑盒模型，可解释性差
- 可能过拟合当前数据分布

预估数据需求: 当前数据量足够 (需去重后使用)

### 方案 D: 模型蒸馏 + 规则学习 (折中)

思路: 用 VLM 作为教师模型，蒸馏到轻量检测器

做法:

1. 用 VLM 对所有 UNSURE 和部分 HAS_SEAL/NO_SEAL 做精细判定
2. 用 VLM 的判定结果 + 现有三模型输出作为特征
3. 训练一个轻量分类器 (XGBoost/小MLP) 做最终判定
4. 输入特征: V2分数, L分数, seal_det碎片数, 颜色blob面积, 圆度, 页数等

优点:

- 无需框级标注
- 可解释 (特征重要性)
- 训练快
- 融合所有信号

缺点:

- 仍依赖三个基础模型推理
- 不是真正的端到端
- 特征工程仍需人工设计

---

## 四、推荐路线

### 短期 (1-2周): 方案 D — 模型蒸馏 + 规则学习

- 用 VLM 跑一批精细标注 (已有 559 条 UNSURE 的 VLM 结果)
- 提取三模型 + 颜色 + 几何特征
- 训练 XGBoost 替代 classify_v5 规则
- 预期: UNSURE 从 2.4% 降到 1% 以下，消除手工阈值

### 中期 (1-2月): 方案 B — 印章专用检测模型

- 标注 2000-5000 张法律文书的印章位置 (可用半自动: 模型预标注 + 人工校正)
- 训练高分辨率 (1280+) 印章检测器
- 替换三个模型，单模型推理
- 预期: 漏检率从 2.4% 降到 1% 以下，推理速度提升 2-3x

### 长期: 方案 C — 端到端二分类

- 积累更多去重后的文档级标注数据
- 设计多页注意力聚合网络
- 完全端到端，无需手工规则和辅助特征
- 预期: 最简洁的部署形态，精度持平或更好

---

## 五、关键瓶颈

1. 训练数据标注成本: 当前只有文档级标签 (有/无印章)，缺乏框级标注。短期可用方案 D 绕过，中期需要标注投入
2. 正负样本不平衡: 去重后 16847 vs 222 (98.7% vs 1.3%)，需要过采样或 focal loss
3. 小印章检测: 固定分辨率输入是根本问题，需要高分辨率输入或多尺度推理
4. 多页文档: 需要设计页级特征聚合机制 (max-pooling / attention)
5. 颜色多样性: 印章有红/蓝/黑/紫等颜色，模型需对所有颜色有效