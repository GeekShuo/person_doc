
Q&A：
为什么配置了代理，，用curl命令等下载文件，代理软件的连接里面没有显示通往目标域名的连接？
可能一：终端用的curl命令下载的时候，并没有经过代理监听的端口，解决方案，强制走代理：
```
curl -L -x http://127.0.0.1:7890

或者环境变量里写
export http_proxy=http://127.0.0.1:7890

export https_proxy=http://127.0.0.1:7890

export all_proxy=socks5://127.0.0.1:7890
```
可能二：实际下载的直链并不是同一个域名


为什么配置了代理后，下载速度还是很慢（259kb）？

尝试一：切换代理，换了后仍然很慢，不是代理问题 
尝试二 用kaggle hub 库和kaggle库下载，仍然龟速，貌似一个ip下载速度最大250Kb？ 使用